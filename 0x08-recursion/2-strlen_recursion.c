#include "main.h"
#include <string.h>
/**
*_strlen_recursion - Prints a length of string
*@s : string
 *
* Return: nothing
*/
int _strlen_recursion(char *s)
{
int cnt;
cnt = strlen(s);
return (cnt);
}
